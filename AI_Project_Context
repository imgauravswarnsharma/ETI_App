# ETI — Expense & Transaction Intelligence  
## Project Context (Authoritative)

---

You are operating inside the **ETI — Expense & Transaction Intelligence** project.

The following artifacts are **AUTHORITATIVE** and must be treated as the **single source of truth**, each within its defined scope, unless explicitly overridden.

---

## 1. AI Operating Contract
- File: `ETI_AI_Operating_Contract.md`
- Role:
  - Defines binding rules for response style, documentation, scripting, versioning, and copy-paste requirements
  - Governs reasoning precedence and intelligence surfaces
  - Must be followed strictly in all responses

---

## 2. Raw Backend Data (Physical Truth)
- File: `ETI_App_v1.3.xlsx`
- Role:
  - Represents the **raw, physical data state**
  - Contains:
    - transaction data
    - master data
    - stored column values
  - Source of truth for:
    - actual data values
    - physical sheet structure as stored
- Explicitly NOT the source of truth for:
  - derived column logic
  - semantic meaning of columns
  - dependency interpretation

---

## 3. Derived Column Logic & Semantic Truth (Primary Reasoning Surface)
- File: `ETI_App_v1.3 - Derived_Column_Logic.csv`
- Role:
  - **Authoritative source for all derived logic**
  - Canonical reference for:
    - column positions (logical)
    - derivation rules
    - dependency chains
    - semantic classification
  - Supersedes:
    - Google Sheets formulas
    - inferred logic from raw backend
- Mandatory usage:
  - Must be consulted before:
    - explaining any column behavior
    - writing or modifying formulas
    - reasoning about dependencies
    - performing optimizations
- Hard rule:
  - If a column or formula is not present or unclear in this CSV, **the AI must ask the user to confirm**
  - No assumptions or inferred logic are allowed

---

## 4. AppSheet Logic & UI Context (Authoritative UI Truth)
- File: `Appsheet_Documentation.pdf`
- Role:
  - Canonical source for:
    - AppSheet formulas
    - views
    - slices
    - actions
    - column visibility and editability
  - Whatever is documented is assumed to be:
    - active
    - deployed
    - in use
- Constraints:
  - AI must not:
    - invent UI logic
    - assume undocumented behavior
  - Any ambiguity must be clarified with the user first

---

## 5. Script Archive (Executable Automation Truth)
- File(s): `*.gs`, `*.js` (partial set due to upload limits)
- Role:
  - Authoritative automation logic
  - Defines:
    - mutation paths
    - execution guarantees
    - failure modes
- Constraints:
  - Not all scripts may be visible
  - AI must not assume missing script behavior
  - If automation behavior is unclear, **ask the user**

---

## MANDATORY BEHAVIOR

- Follow the AI Operating Contract strictly
- Treat uploaded artifacts as authoritative within their defined scope
- Do not infer schema, column order, or logic beyond visible artifacts
- **Never create derived logic by assumption**
- Always confirm column logic from:
  - `ETI_App_v1.3 - Derived_Column_Logic.csv`
  - or directly with the user

All formulas provided must be:
- Copy-paste ready
- Explicitly column-position based
- Mapped back to the derived logic source

---

## WORKING MODE

- Use:
  - Excel → raw data truth
  - Derived_Column_Logic CSV → semantic & logic truth
  - AppSheet documentation → UI truth
  - Scripts → automation truth
- Surface decisions, assumptions, and required confirmations explicitly

---

## DEFAULT ASSUMPTIONS

- ETI v1.3 is active unless explicitly stated otherwise
- Optimize for correctness, auditability, and safety
- No architecture redesign unless explicitly requested

---

Acknowledge this context implicitly and proceed without restating it in future responses.
